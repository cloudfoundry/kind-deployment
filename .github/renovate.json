{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended",
    ":dependencyDashboard"
  ],
  "customManagers": [
    {
      "customType": "regex",
      "managerFilePatterns": [
        "/^docker-bake.hcl$/"
      ],
      "matchStrings": [
        "# renovate: depName=(?<depName>.*?)( registryUrl=(?<registryUrl>.*?))?\\s+default\\s+= \"(?<currentValue>\\d+\\.\\d+\\.\\d+)\""
      ],
      "datasourceTemplate": "github-releases",
      "extractVersionTemplate": "^v?(?<version>.*)$"
    },
    {
      "customType": "regex",
      "managerFilePatterns": [
        "/^values.yaml.gotmpl$/"
      ],
      "matchStrings": [
        "# renovate: depName=(?<depName>.*?)( registryUrl=(?<registryUrl>.*?))?\\s+.+: \"(?<currentValue>\\d+\\.\\d+\\.\\d+)\""
      ],
      "datasourceTemplate": "github-releases",
      "extractVersionTemplate": "^v?(?<version>.*)$"
    },
    {
      "customType": "regex",
      "managerFilePatterns": [
        "/^releases/.*/Chart.yaml$/",
        "/^releases/.*/values.yaml$/",
        "/^values.yaml.gotmpl$/"
      ],
      "matchStrings": [
        "# renovate: depName=(?<depName>.*?) image=(?<packageName>.*?)\\s+appVersion: (?<currentValue>\\d+\\.\\d+\\.\\d+)",
        "# renovate: depName=(?<depName>.*?) image=(?<packageName>.*?)\\s+tag: \"(?<currentValue>\\d+\\.\\d+\\.\\d+)\"",
        "# renovate: depName=(?<depName>.*?) image=(?<packageName>.*?)\\s+.+: \"(?<currentValue>\\d+\\.\\d+\\.\\d+)\""
      ],
      "datasourceTemplate": "docker",
      "extractVersionTemplate": "^v?(?<version>.*)$"
    }
  ],
  "packageRules": [
    {
      "matchManagers": ["helmfile"],
      "matchFileNames": ["helmfile.yaml.gotmpl"],
      "enabled": true
    }
  ]
}
